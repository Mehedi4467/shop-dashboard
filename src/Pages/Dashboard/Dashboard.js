import React from 'react';
import Menu from '../Menu/Menu';

const Dashboard = () => {
    return (
        <div>

            <h1>this is dashboard</h1>
        </div>
    );
};

export default Dashboard;